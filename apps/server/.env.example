NODE_ENV=development
PORT=3002
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/openmeet
REDIS_URL=redis://localhost:6379

# JWT (user)
JWT_ACCESS_SECRET=change-me-access
JWT_REFRESH_SECRET=change-me-refresh
JWT_ACCESS_EXPIRY=7d
JWT_REFRESH_EXPIRY=7d

# Admin (separate from user JWT - short-lived, no refresh)
ADMIN_JWT_ACCESS_SECRET=change-me-admin-access
ADMIN_JWT_ACCESS_EXPIRY=2h

# Chat / corporate messaging
USER_INVITE_TTL_HOURS=168
CHAT_MESSAGE_MAX_LENGTH=8000

# Default admin auto-created on first boot if no admin exists
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=admin12345
DEFAULT_ADMIN_NAME=Example

# Storage
LOCAL_STORAGE_DIR=./uploads
UPLOAD_MAX_SIZE_BYTES=26214400

# Api url
API_PUBLIC_URL=http://localhost:3002

# LiveKit
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret
LIVEKIT_HOST=ws://localhost:7880

# Recording (LiveKit Egress)
RECORDING_EGRESS_FILEPATH_PREFIX=/out
RECORDING_STORAGE_SUBDIR=recordings
RECORDING_LAYOUT=grid

# CORS / front-end origins (user app + admin console)
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001

# Google auth
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3002/api/auth/google/callback

# Mail
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
MAIL_FROM=open-meet <noreply@open-meet.local>
