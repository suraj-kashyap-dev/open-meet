import { ComingSoon } from '@/components/admin/layout/coming-soon';

export const metadata = { title: 'Meetings · Admin' };

export default function AdminMeetingsPage() {
  return (
    <ComingSoon
      title="Meetings"
      description="Browse, search, and moderate every meeting in the system. Coming soon."
    />
  );
}
